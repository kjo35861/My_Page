# 작업내역_체크리스트

> 이 파일은 여러 모델(세션)이 이어서 작업할 때 사용하는 공용 기록입니다.

## 프로토콜 (모델은 아래 순서를 따를 것)

1. **작업 시작 전**: 이 파일 전체를 읽는다. 특히 `다음 작업`과 `체크리스트`의 미완료 항목을 확인한다.
2. **작업 중**: 사용자 지시에 따라 `/mnt/user-data/outputs/index.html`(또는 관련 파일)을 수정한다.
3. **작업 종료 시 (또는 토큰 소진 임박 시)**:
   - `체크리스트`에서 완료된 항목의 `[ ]`를 `[x]`로 바꾼다.
   - `작업 로그`에 새 항목을 추가한다 (날짜, 한 일, 남은 일, 다음 모델에게 전달할 참고사항).
   - `다음 작업`을 실제 남은 일 기준으로 갱신한다.
4. 이 파일 자체를 사용자에게 공개하거나 제출용으로 쓰지 않는다 — 내부 작업 기록용이다. 제출용 "검증 안내서"·"AI 3줄"은 별도 파일로 관리한다.

---

## 프로젝트 개요

- **과제**: 나만의 온라인 포트폴리오 완성 (과제 1)
- **목적**: 회사 채용 감독관에게 보여줄 이력서 성격의 포트폴리오 페이지
- **대상 문장**: 이 페이지는 채용 담당자에게 보여주기 위한 이력서이다.
- **인물**: 김준오
- **산출 파일**: `/mnt/user-data/outputs/index.html`
- **디자인 방향**: 미니멀·깔끔, "해결한 문제 = 이슈 트래커 티켓" 형식의 진단 로그 섹션이 시그니처 요소

---

## 체크리스트 (과제 안내서 기준)

- [ ] 공개 주소가 다른 브라우저에서도 열린다. *(배포 전. GitHub Pages / Netlify / Vercel 중 미정)*
- [x] 대상 문장이 있다. *(위 "대상 문장" 참고)*
- [ ] 공개·비공개 점검표 파일이 별도로 존재한다. *(아직 미작성)*
- [x] 강점/취향 3개가 상황·행동·결과를 채웠다. *("해결한 문제들" 섹션, 3개 티켓)*
- [x] 공개 가능한 근거 1개 이상 연결. *(florography 저장소 링크, 개인 GitHub 프로필 링크)*
- [ ] 두 해상도(1366×768, 1920×1080)에서 핵심 3개가 스크롤 없이 보이고 가로 넘침이 없다. *(직접 테스트 필요)*
- [ ] 결함 3개 이상 수정 전·후 기록 + 콘솔 오류 0건 확인. *(미착수)*
- [~] 상호작용이 마우스·키보드 모두 작동한다. *(티켓 카드에 tabindex/hover 적용됨. 실제 키보드 내비게이션 테스트는 미확인)*
- [x] 공개하지 않기로 한 개인정보(주소·전화번호·이메일)가 0건이다. *(연락처는 사용자 요청으로 추가되었다가 다시 삭제됨)*
- [x] 비밀값(비밀번호·토큰·API 키)이 0건이다.
- [ ] AI 3줄이 별도 제출 파일로 저장되어 있다. *(대화 중 텍스트로만 작성됨, 파일화 필요)*
- [ ] 검증 안내서가 별도 제출 파일로 저장되어 있다. *(대화 중 텍스트로만 작성됨, 파일화 필요)*

---

## 작업 로그

### 2026-08-12 — Claude (session 1)
- **한 일**:
  - `index.html` 최초 제작 (미니멀 이력서 스타일, "진단 로그" 티켓 3개, florography 프로젝트 카드)
  - GitHub 링크 2종 분리 (개인 프로필 `github.com/kjo35861` / 프로젝트 저장소 `github.com/Florography`)
  - 연락처(이메일·전화번호) 추가 → 사용자 요청으로 다시 삭제
  - 검증 안내서 / AI 3줄 초안을 대화 중 텍스트로 작성 (파일로 저장 안 됨)
- **남은 일**: 배포, 공개·비공개 점검표 작성, 해상도·콘솔·키보드 테스트, 결함 수정 전후 기록
- **다음 모델에게**: `index.html`은 완성된 상태지만 미배포. 사용자가 배포 방법(GitHub Pages 등)을 아직 선택하지 않았음. 검증 안내서·AI 3줄을 파일로 만들어 달라는 요청이 오면 위 "작업 로그" 내용을 근거로 작성할 것.

<!--
다음 세션은 아래 형식으로 이어서 기록할 것:

### YYYY-MM-DD — Claude (session N)
- **한 일**:
- **남은 일**:
- **다음 모델에게**:
-->

---

## 다음 작업

1. 배포 방식 결정 및 실행 → 공개 주소 확보
2. 공개·비공개 점검표를 별도 파일(예: `공개범위_점검표.md`)로 작성
3. 검증 안내서, AI 3줄을 각각 제출용 파일로 작성
4. 두 해상도 스크린샷 + 콘솔 오류 0건 스크린샷 확보
5. 키보드 내비게이션(Tab 이동, 포커스 표시) 실제 테스트
